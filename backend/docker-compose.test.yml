services:
  postgres-test:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: getfitbro_test
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data
